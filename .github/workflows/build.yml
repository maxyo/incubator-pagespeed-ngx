name: Building

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Clear
        run: rm -rf *
      - name: Build
        run: wget https://gist.githubusercontent.com/maxyo/b0dbd2c39b056d0039b16048643cf191/raw/c762fb022f365978dc798ae84d2e8b281960930d/pg_install && chmod +x pg_install && ./pg_install -n latest -s -m -y
